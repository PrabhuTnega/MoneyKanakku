# Copy this file to .env and fill in the real password (see README.md's
# "Getting started" section — the isolated dev cluster's credentials are
# created once by initdb and never committed to git).
DATABASE_URL="postgresql://expensio_admin:CHANGE_ME@localhost:5544/expensio_db?schema=public"
